#include <stdio.h>
#include <string.h>

int main() {
    char text[] = "Apple";
    int len = strlen(text);

    for (int i = 0; i < len; i++) {
        printf("%c\n", text[i]);
    }

    return 0;

}
