#include <stdio.h>
#include <string.h>
int main() {
    // Write C code here
    char str[]="MSI";
    int len=strlen(str);
    for(int i=len-1;i>=0;i--){
        printf("%c",str[i]);
    }
    return 0;
}
